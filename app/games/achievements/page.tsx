import { redirect } from "next/navigation"; import { createClient } from "@/lib/supabase/server"; import { Card, CardContent, CardHeader, CardTitle } from "@/components/ui/card"; import { GamesTopbar } from "@/components/games-topbar"
export default async function AchievementsPage(){ const supabase=await createClient(); const {data:{user}}=await supabase.auth.getUser(); if(!user) redirect('/auth/login'); const {data:profile}=await supabase.from('profiles').select('*').eq('id',user.id).single(); const achievements=[['First Spin','Play your first slot round'],['High Roller','Reach 10,000 chips'],['Comeback King','Claim 3 daily rewards in a row']]; return <div className="min-h-svh bg-casino-dark text-white"><GamesTopbar balance={profile?.balance || 0} /><div className="container mx-auto px-6 py-10"><Card className="bg-card border-casino-gold/20"><CardHeader><CardTitle className="text-casino-gold">Achievements (New)</CardTitle></CardHeader><CardContent className="space-y-2">{achievements.map(([title,desc])=><div key={String(title)} className="rounded border border-casino-gold/20 p-3"><p className="text-casino-gold font-semibold">{title}</p><p className="text-casino-silver text-sm">{desc}</p></div>)}</CardContent></Card></div></div> }
