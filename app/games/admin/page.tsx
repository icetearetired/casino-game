import { redirect } from "next/navigation"; import { createClient } from "@/lib/supabase/server"; import { Card, CardContent, CardDescription, CardHeader, CardTitle } from "@/components/ui/card"; import { GamesTopbar } from "@/components/games-topbar"
export default async function AdminPage(){ const supabase=await createClient(); const {data:{user}}=await supabase.auth.getUser(); if(!user) redirect('/auth/login'); const {data:profile}=await supabase.from('profiles').select('*').eq('id',user.id).single(); return <div className="min-h-svh bg-casino-dark text-white"><GamesTopbar balance={profile?.balance || 0} /><div className="container mx-auto px-6 py-10"><Card className="bg-card border-casino-gold/20"><CardHeader><CardTitle className="text-casino-gold">Admin Panel</CardTitle><CardDescription className="text-casino-silver">Moderation and game controls.</CardDescription></CardHeader><CardContent className="text-casino-silver"><ul className="list-disc pl-5 space-y-1"><li>User management (placeholder)</li><li>Economy tuning (placeholder)</li><li>Game event toggles (placeholder)</li></ul></CardContent></Card></div></div> }
