import { redirect } from "next/navigation"; import { createClient } from "@/lib/supabase/server"; import { Card, CardContent, CardHeader, CardTitle } from "@/components/ui/card"; import { Button } from "@/components/ui/button"; import { GamesTopbar } from "@/components/games-topbar"
export default async function ShopPage(){ const supabase=await createClient(); const {data:{user}}=await supabase.auth.getUser(); if(!user) redirect('/auth/login'); const {data:profile}=await supabase.from('profiles').select('*').eq('id',user.id).single(); const items=[['Bronze Boost',500],['Lucky Charm',1200],['VIP Flair',2500]]; return <div className="min-h-svh bg-casino-dark text-white"><GamesTopbar balance={profile?.balance || 0} /><div className="container mx-auto px-6 py-10"><div className="grid md:grid-cols-3 gap-4">{items.map(([name,price])=><Card key={String(name)} className="bg-card border-casino-gold/20"><CardHeader><CardTitle className="text-casino-gold">{name}</CardTitle></CardHeader><CardContent><p className="text-casino-silver mb-3">{price} chips</p><Button className="w-full bg-casino-gold text-casino-dark">Buy (Demo)</Button></CardContent></Card>)}</div></div></div> }
